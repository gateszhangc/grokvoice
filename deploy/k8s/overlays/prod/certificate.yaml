apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grokvoice-live-tls
  namespace: grokvoice
spec:
  secretName: grokvoice-live-tls
  issuerRef:
    kind: Issuer
    name: letsencrypt-prod-cloudflare
  dnsNames:
    - grokvoice.homes
    - www.grokvoice.homes
